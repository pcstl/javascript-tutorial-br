importância: 5

---

# Objetos constantes?

É possível alterar um objeto declarado com `const`, como você acha?

`` `js
const user = {
Nome: "John"
};

*! *
// funciona?
user.name = "Pete";
* /! *
`` `
