importância: 4

---

# Reescreva "if" em "switch"

Reescreva o código abaixo usando uma única instrução `switch`:

`` `js run
deixe a = + prompt ('a?', '');

se (a == 0) {
alerta (0);
}
se (a == 1) {
alerta (1);
}

se (a == 2 || a == 3) {
alerta ('2,3');
}
`` `

